# 要件定義書 - タスク管理アプリ

Trelloを参考にした、個人用のシンプルなタスク管理アプリ。オンラインスクールの課題として開発する。

## 1. 概要

Trelloを参考にした、個人用のシンプルなタスク管理アプリを作成する。オンラインスクールの課題として開発する。

## 2. 対象ユーザー

- 開発者本人（個人利用）
- 複数人での共有・アカウント機能は想定しない

## 3. 利用環境

| 項目 | 内容 |
|---|---|
| 利用デバイス | PC（ブラウザ）のみ |
| スマートフォン対応 | 不要 |
| ログイン機能 | 不要 |
| データ保存先 | バックエンド（Spring Boot）経由でPostgreSQL（Docker Composeで起動）に保存 |

- 個人利用のみを想定するためログイン機能は不要。データはPostgreSQL（Dockerコンテナ）に永続化し、バックエンド・フロントエンドを再起動しても内容が保持される。

## 4. 機能要件

ボード・リスト・カードの操作、データ永続化について。詳細は [docs/機能要件.md](./docs/機能要件.md) を参照。

## 5. 画面一覧

ボード画面と各種モーダルの構成について。詳細は [docs/画面一覧.md](./docs/画面一覧.md) を参照。

## 6. ユースケース

個人ユーザーを起点とした11のユースケース（UC1〜UC11）。詳細は [docs/ユースケース.md](./docs/ユースケース.md) を参照。

## 7. ER図

BOARD・LIST・CARDの関係を示すER図。詳細は [docs/ER図.md](./docs/ER図.md) を参照。

## 8. 非機能要件

| 分類 | 内容 |
|---|---|
| デザイン | 現状のシンプルなTrello風デザイン（白いカード、グレーのリスト背景、横並びのリスト構成）をベースとする。**背景に自然の写真を使用する**（具体的な画像は実装時に選定） |
| 対応ブラウザ | 特に指定なし（開発者本人が利用するブラウザ、最新のChrome想定で動作すればよい） |
| 性能 | カード数が数十〜百件程度でも操作にラグを感じないこと |
| データ永続性 | PostgreSQL（Dockerコンテナ、ボリューム永続化）に保存し、バックエンド・DBが稼働していればブラウザをリロード・再訪しても内容が保持される |
| 可用性 | フロントエンド（Vite開発サーバー等）・バックエンド（Spring Boot）・PostgreSQL（Docker）をローカルで起動している間、利用可能 |
| 保守性 | フロントエンド: React + Vite、バックエンド: Spring Boot + Gradle という指定の技術スタックに沿って実装する（詳細は [10. 技術スタック](#10-技術スタック)） |
| セキュリティ | 個人利用のみのため認証等は不要。ただし、カード入力値（タイトル等）はフロントエンド表示時にエスケープし、不正な文字列で画面が壊れることを防ぐ |

## 9. 制約条件

- 使用技術は [10. 技術スタック](#10-技術スタック) に記載の構成に従う（フロントエンド: React/Vite/dnd-kit/CSS Modules、バックエンド: Java 25 + Spring Boot、API: REST、DB: PostgreSQL（Docker））
- **提出期限: 2026年8月16日（日）ごろまで**（今週中）に素のHTML/CSS/JS版の初期プロトタイプは提出済み。本ドキュメントはその後の技術スタック移行（React + Spring Boot化）に関する改訂版

## 10. 技術スタック

使用技術・利用ブラウザAPI・ファイル構成について。詳細は [docs/技術スタック.md](./docs/技術スタック.md) を参照。

## 11. 今後の検討事項（本要件には含まないが、将来拡張の可能性があるもの）

- 複数デバイス間でのデータ同期（スマートフォン対応）
- ログイン・ユーザーアカウント機能
- 複数ボードの管理
- 期限切れカードの警告表示、期限順の自動並び替え

## 12. 実装状況まとめ

### 素のHTML/CSS/JS版（初期プロトタイプ、React版への移行に伴い置き換え済み）

- [x] カードの追加・削除・編集
- [x] データの永続化（localStorage）
- [x] カードのリスト間移動、ドラッグ&ドロップによる並べ替え
- [x] 期限の設定・表示、優先度の設定・色分け表示
- [x] リストの自由な追加・削除
- [x] 背景に自然の写真を設定

### React + Spring Boot + PostgreSQL版（現行）

環境構築:
- [x] `backend/` にSpring Boot（Gradle, Java 25）の雛形を作成し、動作確認済み
- [x] `frontend/` にReact + Viteの雛形を作成し、dnd-kit・CSS Modulesの動作確認済み
- [x] PostgreSQL接続を一度Postgres.appで確認後、講義方針に合わせて一旦DBなし（インメモリ）構成に変更
- [x] Docker（Docker Compose）でPostgreSQLを起動し、バックエンドから再接続する構成に変更（ポート5433、Postgres.app・5432とは併存）

機能実装（すべてこれから対応）:
- [ ] ボード・リスト・カードのAPI設計（REST API）とDBスキーマ実装
- [ ] カードの追加・削除・編集
- [ ] カードのリスト間移動、ドラッグ&ドロップによる並べ替え（`order`カラムの導入）
- [ ] 期限の設定・表示、優先度の設定・色分け表示
- [ ] リストの自由な追加・削除
- [ ] 背景に自然の写真を設定
